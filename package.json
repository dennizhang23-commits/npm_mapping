{
  "name": "map-routing-git",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd package && npm install",
    "build": "cd package && npm run build",
    "dev": "npm run build && http-server . -p 8080 -o /docs-site/",
    "start": "http-server . -p 8080 -o /docs-site/"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
